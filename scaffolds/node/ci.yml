name: ci
on: [push, pull_request]

jobs:
  ci:
    uses: <OWNER>/<REUSABLE_WORKFLOWS_REPO>/.github/workflows/reusable-node.yml@v1
    with:
      working_directory: "."
      run_typecheck: true
      run_build: true
